- 2026-09-23 14:57 PDT: release notes through the copy model glm-5.3-flash failed 3 times: twice `OpenRouter returned an unsupported message payload (content_type=NoneType, finish_reason=error)` (ids gen-1790199622-ZK7L05Q8EB44DuAHojER, gen-1790199949-RCvLZd7BbjNfmTWbPUsK, first with --read-only-tools and --json-schema, second with --json-schema only), then a 400 s timeout with plain text output. Release left as a draft.
- 2026-09-23 15:16 PDT (orchestrator): I put invented flags (`--route-seed=21`, `--level=0`) into briefs without reading the parser; before a command goes into a brief for several agents, run it once.
- 2026-09-23 15:16 PDT (orchestrator): the shell speed fix was accepted on speed numbers and a menu screenshot; nobody watched the goo move, and it stretched to 3.3 to 4.3 widths. Motion changes need a motion probe, a strip of frames from a clip (Claude cannot watch video), or the `watch` skill through Gemini.
- 2026-09-23 15:16 PDT (orchestrator): the owner's "stop repeating visible runs" became "never open a window" in every brief and blocked fps checks and screenshots; briefs that quoted the owner verbatim did not have this problem.
- 2026-09-23 15:16 PDT (orchestrator): the Blender MCP was called set up without one real tool call; it sat at `claude mcp get` "Pending approval" for a day. The preinstall-check health step could run `claude mcp get <name>` plus one tool call through `claude -p`, which needs no new session.
- 2026-09-23 15:19 PDT: a runtime SCRIPT ERROR inside a called function of scripts/check_bodies.gd (`Nonexistent function 'map' in base 'PackedVector3Array'`) skipped the rest of that function yet the check still printed `BODY_CHECK_OK=true checks=43` and exited 0; a check that dies mid-way passes silently.
- 2026-09-23 15:17 PDT: `OS.get_cmdline_args()` hides Godot's own flags such as `--fixed-fps 60` (it returned only `["--script", ...]`), and Godot 4.7.2 has no query for a fixed frame rate; drive_levels.gd reads `ps -o command=` for its own pid instead.

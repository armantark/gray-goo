- 2026-09-23 00:22 PDT (layout-leftovers) A SceneTree probe that built GameWorld directly in `_initialize` printed `ERROR: Condition "!is_inside_tree()" is true. Returning: Transform3D()` thousands of times (20.1 MB of output); build worlds from a `call_deferred("_run")` like scripts/check_worlds.gd does.
- 2026-09-23 00:23 PDT (layout-leftovers) Judging "draws nothing" right inside `GameWorld.advance_scale` is too early: Sugar Water rebuilds each nucleus's nucleon batch in its layout `step`, so at that moment 2 Helium nuclei at tier 3 look empty. Check after `_layout.step`.
- 2026-09-23 00:26 PDT (layout-leftovers) The Skatepark "pale tongue" was not a decal or mesh but shaders/ground.gdshader's hard `facing > 0.55` light band crossing the curved bowl walls under the low fill light.
- 2026-09-23 01:05 PDT (ticket-13) Local PinchTab 0.14.1 (headless instance, Chrome 154.0.8037.58 while config pins 144.0.7559.133) fails every screenshot and capture with `Error 500: {"code":"error","error":"screenshot: Unable to capture screenshot (-32000)"}`, even on a plain directory listing and after `pinchtab server restart`; snap and nav still work.
- 2026-09-23 01:06 PDT (ticket-13) Workaround that worked: copy dist/ and scripts/web_build.py to the laptop (`tar czf - dist scripts/web_build.py | ssh ArmanTarkhanian@armans-macbook-pro 'cd /tmp/graygoo-release && tar xzf -'`), serve there, and drive its headless PinchTab 0.15.0, whose screenshots work; Godot's canvas takes CDP ArrowKey keydown/keyup and `mouse down --x --y` (coordinates must be flags, not positional) in CSS pixels. Also: `screencapture` on this Mac fails with `could not create image from display` (no Screen Recording grant); `--write-movie <dir>/frame.png --fixed-fps 30 --quit-after 90` on the exported app binary captures frames instead.
- 2026-09-23 08:38 PDT (momentum) Making spawn points release only out of view starved Tide Pool and Skatepark in view 0 (procedural routes stuck at 68.2 s and 92.9 s, `"stuck": true`): the route driver had parked on a source that was now always in view. A source in view must send its mover along its path until the path leaves the view, not just wait.
- 2026-09-23 08:56 PDT (momentum) In Skatepark views 3 and 4 the camera shows almost the whole field, so the gate source at (46, 19) could never be out of view and every cart retired unreleased (procedural route: view 3 with 0 meals, gap 30.0 s). The camera never shows past the field edge, so a source there is always out of view; `GameWorld._move` now retires a mover only when it leaves the field from inside.
- 2026-09-23 08:58 PDT (momentum) A full Sugar Water route with `--simulation-clock` runs at about 1 simulated second per wall second, so one route costs 6 to 10 minutes; start both bodies in parallel and read `builds/.../route-*.log` instead of rerunning.
- 2026-09-23 10:00 PDT (momentum) `scripts/check_bodies.gd` printed `BODY_CHECK_OK=true checks=72` right after `ERROR: BODY_CHECK_FAIL no Helium nucleus` and three `SCRIPT ERROR: Invalid access ... on a base object of type 'Nil'`: `_find` only push_errors, and the script errors abort the case before its `_check` calls, so the count drops instead of the verdict. Compare the check count (88 when every case runs) and grep for SCRIPT ERROR, not just the OK line.
- 2026-09-23 09:13 PDT (momentum) The shell body moves at 0.49 to 0.52 of its commanded eight body lengths per second at radius 0.2 to 2.5; the procedural body reaches 0.92 (straight 2 s drive probe). With constant momentum, route time is mostly travel, so Sugar Water's shell routes run 1.5 to 1.8 times the procedural ones and both cannot sit inside 360 to 600 s at once.
- 2026-09-23 11:02 PDT: routes ran at real-time speed despite `--simulation-clock`: headless Godot sleeps `OS.low_processor_usage_mode_sleep_usec` (about 6.9 ms) per frame, capping it near 144 fps, and the delta followed the wall clock. Also, in zsh a variable holding `--fixed-fps 60` passes as ONE argument that Godot ignores; write the flag literally. Fixed with sleep 0, `--fixed-fps 60`, and scripts/route.sh.
- 2026-09-23 11:11 PDT (shell-speed) `scripts/route.sh` ran all four routes and then printed `SyntaxError: unexpected character after line continuation character` from its summary: the `\"` escapes inside the single-quoted `python3 -c` f-string reach Python as backslashes in expression code. The route JSON was fine; the summary now uses % formatting.
- 2026-09-23 11:05 PDT (shell-speed) Probing two `goo_body.gd` variants in parallel in one checkout raced: each patched and `git checkout`-restored the same file, so both results were invalid. Variant probes that edit source must run sequentially or in separate worktrees.

# Five-anchor motion

The user wants one quarter of the current default speed, the existing trailing adhesion, forward reaching and gripping with five irregular anchors, and eyes that follow the edible target during keyboard control or the cursor during mouse control.

Body leaf owns only src/goo_body.gd and leaf-prefixed scratch probes. Restore continuous shell reaching with five independent, irregular headings and cycles; retain exact material advection and previous-flow subtraction, boundary drive stops, and current trailing contact behavior. Never add separate appendage meshes. Add public gaze_screen_position: Vector2 and use it instead of the viewport mouse position in eye rendering. Root supplies that position each frame.

Root owns camera_rig.gd, game.gd, documentation, recording, export, native UI and git. The camera remembers whether the latest steering input was mouse or keyboard, with keyboard priority. Game uses the existing arrow target to select screen gaze. With no target, keyboard gaze rests near the body. Speed multiplier becomes 2.5 from 10.0; saved percentage remains valid.

Body acceptance: five irregular active reach cycles visibly deform the same shell forward and plant; rear stretch remains; finite body during ordinary motion, stops, turns, growth and boundary contact. CHECK: production Godot headless import plus a bounded actual-body probe. EXPECT: no script errors or unbounded positions; each reach cycle activates. Root verifies native appearance and integrated controls, then exports and records a comparison clip. No permanent test matrix or additional external review. No pushes, destructive git, config edits, installs, or unrelated scene changes. Agents do not commit or operate shared native UI.

User addition: "And when you're not moving the goo will slowly melt into an oozing puddle, like you had in that one image gen way earlier". Body leaf also owns gradual idle flattening/spreading and smooth gathering on resumed movement. Keep one connected, volume-preserving shell with eyes attached to its changing surface. Add idle/resume to the bounded body check.

Calibration: simply quartering the drive produced only 0.12663098971871 of prior travel because forward adhesion absorbs drive. A four-second production-body comparison found drive12.0 produced31.5433190785116 versus125.385723803542 prior units, ratio0.25157025952918. Root selects multiplier3.7 (default11.988 at radius.55) to meet the user’s requested travel pace.

User correction: "The idle melt should be less uniform. Make it look like a randomized ooze". The idle footprint must have unequal, slowly spreading broad lobes, with no frame-to-frame noise or regular gait. Preserve volume and regrouping. This supersedes uniform flattening as a visual target.

Verification complete: body leaf delivered; root reran production stability/plant/melt/resume checks at final default and maximum slider, verified control routing and final approximate quarter travel, inspected native and recorded views, and exported the app. The revised recording includes a seven-second idle interval. The provisional leaf asymmetry percentage is recorded as a visual measurement, not a product gate. No requested behavior was abandoned.
